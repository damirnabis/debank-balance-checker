# Number of simultaneous streams (when a proxy is available) | Количество одновременных потоков (при наличии прокси)
MAX_CONCURRENT = 5

# Number of attempts per proxy, after which it tries the next proxy | Количество попыток на один прокси, после чего пытается через следующий прокси
ATTEMPTS_PER_PROXY = 3

# Pause between data updates from Debank (minutes) | Пауза между обновленияем данных из Debank (минуты)
UPDATE_DATA_MIN = 15

# Pause between data updates on the browser page (seconds) | Пауза между обновленияем данных на странице браузера (секунды)
UPDATE_HTML_SEC = 5